<?xml version="1.0"?>
<!-- I would use maven but too many directories :(. I guess I'll have to deal with XML -->
<project name="PSApp" default="build" basedir=".">
	<target name="build">
		<javac srcdir="src" destdir="build" debug="true" includes="**/*.java"/>
	</target>
</project>	
